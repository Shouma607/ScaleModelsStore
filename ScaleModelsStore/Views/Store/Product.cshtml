@model ScaleModelsStore.Models.Product

@{ 
    ViewBag.Title = "Product details";
}

@*<script type="text/javascript">
    $(function () {        
        $.ajaxSetup({ cache: false });
        $('#to-cart').click(function (e) {
            e.preventDefault();
            var options = { "backdrop": "static", keyboard: true };
            $.get(this.href, function (data) {
                $('#dialogContent').html(data);
                $('#modDialog').modal(options);
                $('#modDialog').modal('show');
            });
        });           
    })
</script>*@


<h2>@Model.ProductName</h2>

<p>
    <img alt="@Model.ProductName" src="@string.Format("{0}placeholder.gif",Model.ImagePath)" />
</p>
<div>
    <p>
        <h4>Category:</h4>
        @Model.Category.Name
    </p>
    <p>
        <h4>Manufacturer:</h4>
        @Model.Manufacturer.Name
    </p>
    <p>
        <h4>Scale:</h4>
        @Model.Scale
    </p>
    <p>
        <h4>Material:</h4>
        @Model.Material
    </p>
    <p>
        <h4>Description</h4>
        @Model.Description
    </p>
    <p>
        <h4>Price:</h4>
        @string.Format("{0:F}", Model.Price)
    </p>
    <p>        
        <a class="sms-button" id="to-cart" href="@Url.Action("AddToCart","ShoppingCart",new { id=Model.ProductId})" title="To Cart"><span>Add to cart</span></a>
        
        <a class="sms-button" href="@Url.Action()" title="Add to Wish List"><span>To wish list</span></a>
    </p>

    @*<div id="modDialog" class="modal">
        <div class="modal-dialog">
            <div class="modal-content">
                <div id="dialogContent"></div>
            </div>
        </div>
    </div>*@

</div>
